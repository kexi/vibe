#!/usr/bin/env node

/**
 * vibe CLI entry point for Node.js
 *
 * This script serves as the entry point when running vibe via npx or npm.
 * It simply imports and runs the main module.
 */

import "../dist/main.js";
